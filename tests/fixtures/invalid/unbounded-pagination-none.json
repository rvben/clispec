{
  "x-test": {
    "violates": "cardinality unbounded cannot pair with pagination style none",
    "detect": "schema",
    "at": "/commands/0/pagination/style",
    "why": "Closes the obvious escape hatch: declaring unbounded and then declaring it unpageable would satisfy the required-property rule while leaving the consumer with no way to read the result safely."
  },
  "clispec": "0.3",
  "name": "mytool",
  "version": "1.0.0",
  "commands": [
    {
      "name": "list",
      "description": "List every event ever recorded.",
      "effects": "read_only",
      "cardinality": "unbounded",
      "stdout_schema": {},
      "pagination": {
        "style": "none"
      },
      "fields_arg": "--fields",
      "args": [
        {
          "name": "--fields",
          "type": "string[]"
        }
      ]
    }
  ],
  "errors": [
    {
      "kind": "not_found",
      "exit_code": 4
    }
  ]
}
