{
  "x-test": {
    "violates": "output_kind opaque cannot declare output_fields",
    "detect": "schema",
    "at": "/commands/0/output_fields",
    "why": "Bytes have no field structure. Allowing both would let a document claim an exemption from structured output while still advertising a record shape, which is the contradiction the kinds exist to prevent."
  },
  "clispec": "0.3",
  "name": "mytool",
  "version": "1.0.0",
  "commands": [
    {
      "name": "download",
      "description": "Write an artifact to stdout.",
      "effects": "read_only",
      "output_kind": "opaque",
      "media_type": "application/octet-stream",
      "output_fields": [
        {
          "name": "size",
          "type": "integer"
        }
      ]
    }
  ],
  "errors": [
    {
      "kind": "not_found",
      "exit_code": 4
    }
  ]
}
