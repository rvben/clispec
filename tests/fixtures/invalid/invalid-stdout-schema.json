{
  "x-test": {
    "violates": "stdout-schema-valid",
    "detect": "rules",
    "at": "/commands/0/stdout_schema",
    "why": "An unparseable stdout_schema is worse than none, because it reads as an authoritative description of the output. JSON Schema cannot validate an embedded schema, so the checker compiles it."
  },
  "clispec": "0.3",
  "name": "mytool",
  "version": "1.0.0",
  "commands": [
    {
      "name": "list",
      "description": "List services.",
      "effects": "read_only",
      "cardinality": "bounded",
      "stdout_schema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "arrray"
          }
        }
      }
    }
  ],
  "errors": [
    {
      "kind": "not_found",
      "exit_code": 4
    }
  ]
}
